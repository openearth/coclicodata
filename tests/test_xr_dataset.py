import xarray as xr


def test_crs(ds):
    """TODO: test if 'epsg' or 'proj:espg' in ds.coords or 'crs' in ds.attrs or spatial_ref
    object in ds.coords/attrs.

    Args:
        ds (_type_): _description_
    """
    raise NotImplementedError
